const HOST_DOMAIN = import.meta.env.DEV && 'https://fakestoreapi.com';

const API_URL = `${HOST_DOMAIN}/`;
const PROJECT_NAME = 'DevTask';
export { API_URL, HOST_DOMAIN, PROJECT_NAME };
export default { API_URL, HOST_DOMAIN, PROJECT_NAME };
